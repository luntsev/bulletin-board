package layout

import "fmt"

css headerClass(width int) {
    background: { "#222222" };
    width: { fmt.Sprintf("%d%%", width) };
    text-align: { "center" };
}

templ Header() {
    <div class={ headerClass(50) }>
        { children... }
    </div>
}